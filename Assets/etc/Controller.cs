using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Controller : MonoBehaviour
{
    //public static float speed = 5f;

    ////private void Awake()
    ////{
    ////    if (GameSet.controllerInstance == null) GameSet.controllerInstance = this;
    ////}

    //// Start is called before the first frame update
    //void Start()
    //{
        
    //}

    //// Update is called once per frame
    //void Update()
    //{
    //    ////Vector3 vec = new Vector3(0, 0.1f, 0);
    //    //Vector3 vec = new Vector3(Input.GetAxis("Horizontal"), 0, Input.GetAxis("Vertical"));
    //    //vec *= speed;
    //    //vec *= Time.deltaTime;
    //    ////벡터값을 현재위치에 더해줌
    //    //transform.Translate(vec);
    //}

    ////public Vector3 movePosition(Transform instanceTransform)
    ////{
    ////    Vector3 movement = new Vector3(Input.GetAxis("Horizontal"), 0, Input.GetAxis("Vertical"));
    ////    movement *= speed;
    ////    movement *= Time.deltaTime;

    ////    return movement;

    ////    //instanceTransform.Translate(movement);
    ////}


}
